import { MobileNavItem } from '@layouts/mobile/mobile-nav-item';

/**
 * Public website desktop navigation.
 * Use a dedicated nav type when desktop chrome is built; MobileNavItem works as a placeholder.
 */
export const WEBSITE_DESKTOP_NAV_ITEMS: MobileNavItem[] = [];
