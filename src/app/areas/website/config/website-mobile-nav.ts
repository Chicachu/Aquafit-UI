import { MobileNavItem } from '@layouts/mobile/mobile-nav-item';

/** Public website mobile navigation. Populate when /mobile routes are enabled. */
export const WEBSITE_MOBILE_NAV_ITEMS: MobileNavItem[] = [];
