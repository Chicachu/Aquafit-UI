/**
 * Test environment. Used when building from the test branch for deploy to test.aquafitvallarta.com.
 * Set apiUrl to your Koyeb test API service URL once it exists.
 */
export const environment = {
  production: true,
  apiUrl: 'https://YOUR-TEST-API-SERVICE.koyeb.app/api',
};
